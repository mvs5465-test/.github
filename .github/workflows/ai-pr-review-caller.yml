name: AI PR Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  ai_review:
    permissions:
      contents: read
      pull-requests: write
      issues: write
    uses: ./.github/workflows/ai-pr-review.yml
    secrets: inherit
