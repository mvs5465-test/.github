name: Codex PR Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  codex_review:
    permissions:
      contents: read
      pull-requests: write
      issues: write
    uses: ./.github/workflows/codex-pr-review.yml
    secrets: inherit
